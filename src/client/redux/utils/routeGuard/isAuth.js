export default function checkLogin(nextState, replace) {
  console.log(nextState);
  console.log(replace);
  // console.log(typeof localStorage === 'undefined');
  // console.log(typeof localStorage.getItem('token') === 'undefined');
  // if (typeof localStorage === 'undefined' || typeof localStorage.getItem('token') === 'undefined') {
  //   replace('/');
  // }
}
